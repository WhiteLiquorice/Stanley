function createMonitoringRouter({ express, service, requireUser }) { const router = express.Router(); router.get('/alerts', async (req, res) => { try { const uid = await requireUser(req); res.json({ success: true, alerts: await service.store.listAlerts(uid) }); } catch (error) { res.status(422).json({ success: false, error: error.message }); } }); router.get('/workflows/:workflowId', async (req, res) => { try { const uid = await requireUser(req); const records = await service.store.listRecords(uid, req.params.workflowId); res.json({ success: true, evaluation: require('./policy').evaluateWindow(records, await service.policyFor(uid, req.params.workflowId)), records }); } catch (error) { res.status(422).json({ success: false, error: error.message }); } }); return router; }
module.exports = { createMonitoringRouter };
