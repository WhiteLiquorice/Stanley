const test = require('node:test'); const assert = require('node:assert/strict');
const { createOAuthConnectionDefinition, importOpenApi, inspectPythonSource } = require('../src');
test('imports fixed OpenAPI operations as tenant connector drafts', async () => { const [artifact] = importOpenApi({ openapi: '3.1.0', info: { title: 'Example' }, servers: [{ url: 'https://api.example.com/v1/' }], paths: { '/items': { get: { operationId: 'listItems', summary: 'List items' } } } }, { tenantId: 'tenant-1' }); assert.equal(artifact.connectorId, 'openapi_listItems'); assert.equal(artifact.readWrite, 'read'); assert.equal((await inspectPythonSource(artifact.source, artifact)).ok, true); });
test('creates secret-reference-only OAuth definitions', () => { const definition = createOAuthConnectionDefinition({ connectionId: 'crm', tenantId: 'tenant-1', authorizationUrl: 'https://auth.example.com/authorize', tokenUrl: 'https://auth.example.com/token', clientIdVaultRef: 'crm.client_id', clientSecretVaultRef: 'crm.client_secret', scopes: ['write', 'read'] }); assert.equal(definition.accessTokenVaultRef, 'crm.access_token'); assert.deepEqual(definition.scopes, ['read', 'write']); });
