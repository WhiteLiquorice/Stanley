const test = require('node:test'); const assert = require('node:assert/strict'); const fs = require('fs'); const path = require('path');
const { applyConnectorHooks } = require('../scripts/patchConnectorHooks');
test('replaces the unsafe global cache and generation fallback deterministically', () => { const source = fs.readFileSync(path.resolve(__dirname, '../../../branchingEngine.js'), 'utf8').replace(/\r\n/g, '\n'); const patched = applyConnectorHooks(source); assert.match(patched, /Executed approved connector/); assert.doesNotMatch(patched, /collection\('global_api_scripts'\)/); assert.doesNotMatch(patched, /generatePythonApi\(goal/); assert.equal(applyConnectorHooks(patched), patched); });
