# Stanley Memory Engine

Tenant-isolated procedural, semantic, and episodic memory with explicit provenance, confidence, retention, approval, deterministic retrieval, conflict explanations, and deletion. It uses structured matching rather than embeddings or another model call.
