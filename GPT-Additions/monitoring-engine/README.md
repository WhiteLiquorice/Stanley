# Stanley Outcome Monitoring

Rolling workflow SLOs for verified success, latency, execution cost, model calls, and consecutive failures. Breaches create tenant-scoped alerts and can pause a workflow or invoke an approved component rollback callback.
