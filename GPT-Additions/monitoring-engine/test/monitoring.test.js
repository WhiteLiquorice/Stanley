const test = require('node:test'); const assert = require('node:assert/strict'); const { MonitoringService, MonitoringStore } = require('..'); test('opens an alert and invokes controls on a consecutive-failure breach', async () => { let controls = 0; const service = new MonitoringService({ store: new MonitoringStore(), policyFor: async () => ({ minRuns: 10, maxConsecutiveFailures: 3 }), onBreach: async () => { controls += 1; } }); for (let i = 0; i < 3; i++) await service.record('u', { workflowId: 'w', runId: `r${i}`, success: false }); assert.equal(controls, 1); assert.equal((await service.store.listAlerts('u'))[0].severity, 'critical'); });
