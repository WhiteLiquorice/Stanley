# Connector workbench

The isolated operator view supports connector generation, inspection, regression
testing, approval, publication, health metrics, version history, and rollback.
`website-overlay/patchWebsite.js` adds protected routes and navigation only when
the additions are promoted into a staging website build.
