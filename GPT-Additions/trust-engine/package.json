{
  "name": "stanley-trust-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic trust, evidence, checkpoint, and exception primitives for Stanley",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "check": "node --check src/index.js && node --check src/assertions.js && node --check src/evidence.js && node --check src/resume.js && node --check src/trustPolicy.js && node --check src/trustStore.js && node --check src/trustRuntime.js && node --check src/trustRoutes.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
